# Minimal go-broadcast configuration
# This is the simplest possible configuration for syncing files

version: 1

mappings:
  - source:
      repo: "org/template-repo"
      branch: "master"
      id: "main"
    targets:
      - repo: "org/target-repo"
        files:
          - src: ".github/workflows/ci.yml"
            dest: ".github/workflows/ci.yml"
